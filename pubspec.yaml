name: app_aluno
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  lottie: ^1.4.3
  sqflite:
  shared_preferences:
  http:
  path:
  location: ^5.0.0
  flutter_map:
  latlong2:
  permission_handler:
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/mark-laranja.png
    - assets/images/animation.json    
    - assets/images/TelaFundo.jpg

